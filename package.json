{
  "name": "cloudflare-cli",
  "version": "1.0.0",
  "description": "Cloudflare DNS CLI and API client.",
  "type": "module",
  "packageManager": "bun@1.4.0",
  "bin": {
    "cloudflare-cli": "./bin/cloudflare-cli"
  },
  "scripts": {
    "check": "bun run typecheck && bun run test",
    "typecheck": "tsc --noEmit",
    "test": "bun test ./src"
  },
  "devDependencies": {
    "@types/bun": "1.4.0",
    "typescript": "7.0.2"
  }
}
